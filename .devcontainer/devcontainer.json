{
  "name": "Master-Bot Prisma DB Infrastructure",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "db_main",
  "workspaceFolder": "/workspace",
  "forwardPorts": [
    6433,
    6432,
    6379
  ],
  "portsAttributes": {
    "5432": {
      "label": "Master-Bot Core DB Engine",
      "onAutoForward": "notify"
    },
    "5433": {
      "label": "Master-Bot Shadow DB Engine",
      "onAutoForward": "notify"
    },
    "6379": {
      "label": "Master-Bot Shared Redis Cache",
      "onAutoForward": "notify"
    }
  },
  "containerEnv": {
    "DB_USER": "${localEnv:DB_USER}",
    "DB_PASSWORD": "${localEnv:DB_PASSWORD}",
    "DB_NAME": "${localEnv:DB_NAME}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "github.vscode-pull-request-github"
      ]
    }
  },
  // Automatically runs the GitHub CLI utility right after startup to force visibility rules to update
  "postStartCommand": "gh codespace ports visibility 5432:public 5433:public 6379:public"
}
